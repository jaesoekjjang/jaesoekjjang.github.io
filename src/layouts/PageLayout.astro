---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Favicon from "./Favicon.astro";
import CenteringWrapper from "./CenteringWrapper";
import { ViewTransitions } from "astro:transitions";

export interface Props {
  width?: string;
}
const { width } = Astro.props;
---

<!DOCTYPE html>
<html class="theme-sleek" lang="en">
  <head>
    <!-- Global Metadata -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="generator" content="Blogster" />
    <ViewTransitions>
      <Favicon />
      <slot name="meta" />
    </ViewTransitions>
  </head><body class="text-text-body bg-bg-body mx-auto">
    <Header />
    <div class="main-wrapper h-[calc(100%-theme(space.16))]">
      <main id="main">
        <slot name="main" />
      </main>
    </div>
    <style>
      body {
        min-height: 100vh;
      }

      .main-wrapper {
        position: relative;
        min-height: 100%;
      }
    </style>
  </body>
</html>
