<article
  class="prose
    prose-h1:text-4xl prose-h1:font-semibold
    prose-h2:text-2xl prose-h2:font-semibold prose-h2:mt-10 prose-h2:mb-5
    prose-h3:text-xl prose-h3:font-semibold
    prose-h4:text-lg prose-h4:font-semibold
    prose-p:my-5
    prose-a:text-[--link]
    prose-img:rounded-sm
    prose-pre:rounded-sm"
>
  <slot />
</article>

<style is:inline>
  .prose {
    code {
      font-family: Consolas, Monaco, "Andale Mono", monospace;
      font-size: 0.75rem;
      font-weight: 400;
    }

    code:not(pre > code) {
      padding: 4px;
      border-radius: 4px;
      background-color: var(--inline-code-bg);
      color: var(--inline-code-text);
    }

    code::before {
      content: "";
    }

    code::after {
      content: "";
    }

    h2:before {
      content: "";
      display: block;
      height: 60px; /* fixed header 높이 만큼 부여 or 브라우저 상단에서 띄워놓기 원하는 높이 */
      margin-top: -60px; /* 위에서 설정한 높이와 동일한 만큼을 음수로 제공 */
      visibility: hidden;
    }
  }
</style>
